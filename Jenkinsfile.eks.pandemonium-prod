pipeline {
    agent any
    stages {
        stage("Checkout code") {
            steps {
                checkout scm
            }
        }
        
        stage ("Download .env file from s3") {
            steps{
                withAWS(region:"ap-south-1", credentials:"AWS-S3-ENV"){
                    s3Download(file:'.env', bucket:'likeminds-configs-prod', path:'application-dot-envs-prod/pandemonium-prod/pandemonium-prod-dot-env-private', force: true)
                }
            }
        }
        
        stage ('Build Image') {
            steps {
                sh "sudo docker build -f Dockerfile.pandemonium -t 523792672653.dkr.ecr.ap-south-1.amazonaws.com/likeminds-pandemonium-repo:pandemonium-V$BUILD_NUMBER ."
            }
        }
        
        stage ('Push Image') {
            steps {
                sh "aws ecr get-login-password --region ap-south-1 | sudo docker login --username AWS --password-stdin 523792672653.dkr.ecr.ap-south-1.amazonaws.com"
                sh "sudo docker push 523792672653.dkr.ecr.ap-south-1.amazonaws.com/likeminds-pandemonium-repo:pandemonium-V$BUILD_NUMBER"
            }
        }
        
        stage ('Remove Image from jenkins') {
            steps {
                sh "sudo docker rmi 523792672653.dkr.ecr.ap-south-1.amazonaws.com/likeminds-pandemonium-repo:pandemonium-V$BUILD_NUMBER"
            }
        }

        stage ("update tag and push helm charts") {
          steps {
             script{
                  git branch: 'LM-10466-aws-migration', url: 'git@github.com:LikeMindsCommunity/LikeMinds-Infra-Config.git'
                  sh "ls"
                  sh 'sed -i "s/tag: pandemonium-V[0-9]*/tag: pandemonium-V${BUILD_NUMBER}/g" ./prod/pandemonium-prod/values.yaml'
                  sh "git add ./prod/pandemonium-prod/values.yaml"
                  sh "git status"
                  sh 'git commit -m "CI/CD pipeline updated $IMAGE_TAG image to new image tag"'
                  sh 'git config --global credential.helper cache'
		          sh "git push origin LM-10466-aws-migration"
                }
            }
        }

    }
}
